请勿将完整权重文件直接提交到 Git 仓库。

本目录用于说明如何获取与放置模型权重：

1. 权重位置（本地）：将权重放在 `checkpoints/checkpoints_hybrid_v2/` 目录下（示例目录已经包含一个占位文件 `epoch_2.pt`）。
2. 下载说明：请联系仓库维护者获取下载链接或使用私有存储服务（如 S3、GCS）；在公开仓库中仅放置小的元数据或下载脚本。
3. 如果希望将权重发布到 Hugging Face Hub，请在 `MODEL_CARD.md` 中记录上传策略并在 README 中给出 Hugging Face 模型链接。
